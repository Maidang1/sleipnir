# M5 notes

## Goal

Repeatable **ad-hoc upstream port process** without git submodule of Zed.

## Deliverables

| Item | Path |
|------|------|
| Policy + checklist | `UPSTREAM.md` |
| Dry-run script | `scripts/upstream-diff.sh` |
| Last dry-run report | `docs/upstream-last-diff.txt` |

## First dry-run (M5 complete)

- **jiajia** `8391e5e` (at dry-run time)
- **Zed** `371a7d4` (2026-08-09)
- **alacritty** rev matches Zed (`4c129667…`)
- **Identical:** `gpui/src`, `gpui_macos/src`, `collections/src`, `util/src`
- **Expected forks:** `terminal/src` (3 files), `gpui_platform/src` (macOS-only slim)

No code port was required (baseline still aligned except intentional forks).

## Commands

```bash
export ZED_ROOT=~/codes/open-source/zed   # or your checkout
./scripts/upstream-diff.sh "$ZED_ROOT"
cargo build -p jiajia_term
cargo run -p jiajia_term
```
