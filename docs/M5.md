# M5 notes

## Goal

Repeatable **upstream upgrade** without vendoring the full GPUI tree.

## Model (current)

| Item | Path / mechanism |
|------|------------------|
| Policy + checklist | `UPSTREAM.md` |
| GPUI / shared utils | Cargo git deps → `zed-industries/zed` @ pinned `rev` |
| Local forks | `crates/terminal`, `crates/gpui_platform`, sleipnir app crates |
| Dry-run script | `scripts/upstream-diff.sh` (forks + pin listing) |
| Last dry-run report | `docs/upstream-last-diff.txt` |

## Pin at hybrid switch

- **Zed** `371a7d4ba2fd0064b79a0bc67d28e57a906779dc`
- **alacritty** rev `4c129667ce56611becdc82de6e28218c80e2e88f` (matches Zed)
- Vendored `crates/gpui*` removed; build via git pin verified: `cargo build -p sleipnir`

## Commands

```bash
export ZED_ROOT=~/codes/open-source/zed   # optional, for fork diffs
./scripts/upstream-diff.sh "$ZED_ROOT"
cargo build -p sleipnir
cargo run -p sleipnir
```
