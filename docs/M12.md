# M12 notes

**Status:** planned (not implemented) — **next product milestone**

## Goal

Close daily-use gaps versus Kaku / Kitty / Ghostty while keeping Sleipnir a clean macOS terminal (no AI, no shell-suite installer).

## Scope

| # | Feature | User-facing result |
|---|---------|-------------------|
| 1 | Font zoom | `⌘+` / `⌘-` / `⌘0` adjust size at runtime (window-scoped override; not persisted by default) |
| 2 | New OS window | `⌘N` opens an independent window |
| 3 | Close confirm | Closing pane/tab/window with a foreground job prompts once |
| 4 | Path open | Cmd-click path-like targets (`file.rs:12`, absolute/relative paths) |
| 5 | Bell | `terminal.bell`: `off` \| `system` \| `visual` actually works |
| 6 | copy_on_select UX | Toggle visible in settings UI + docs (default remains `false`) |

## Planned settings (not in code yet)

```json
{
  "confirm_close": "dirty",
  "path_links": true,
  "terminal": {
    "bell": "visual",
    "copy_on_select": false
  }
}
```

| Key | Values | Default (planned) | Meaning |
|-----|--------|-------------------|---------|
| `confirm_close` | `dirty` \| `always` \| `never` | `dirty` | Confirm close when a non-shell foreground job is running |
| `path_links` | bool | `true` | Open path-like navigation targets |
| `terminal.bell` | `off` \| `system` \| `visual` | `off` (today) | System beep and/or tab flash |
| `terminal.copy_on_select` | bool | `false` | Already implemented; M12 makes it discoverable |

**Dirty policy:** idle interactive shell alone is **not** dirty; a foreground job (`sleep`, `npm`, `vim`, …) is dirty.

## Planned shortcuts

| Key | Action |
|-----|--------|
| `⌘+` / `⌘=` | Increase font size |
| `⌘-` | Decrease font size |
| `⌘0` | Reset font size to settings |
| `⌘N` | New window |

## Non-goals (M12)

- Pane zoom, unfocused split dim, broadcast (→ M13)
- Shell integration / OSC 133 / command-finish notify (→ M14)
- Quick Terminal, Quick Select (→ M15)
- Changing default `copy_on_select` to `true`
- Multi-window session restore (document last-window-wins if needed)

## Implementation plan

Task-level checklist (files, steps, acceptance):

[`docs/superpowers/plans/2026-08-12-post-m10-feature-roadmap.md`](superpowers/plans/2026-08-12-post-m10-feature-roadmap.md) — section **M12**

Research input:

[`docs/competitive-research-features.md`](competitive-research-features.md)

## Implementation map (when started)

| Piece | Location |
|-------|----------|
| Settings keys | `crates/sleipnir_settings` |
| Font zoom + close confirm UI | `crates/sleipnir_ui/src/app_shell.rs` |
| Path open / bell bubble | `crates/sleipnir_ui/src/sleipnir_ui.rs` |
| Key bindings + new window | `crates/sleipnir/src/main.rs`, `app_menus.rs` |
| Busy process query | `crates/terminal` / `pty_info` |

## Acceptance (release gate)

- [ ] Font zoom reflows PTY without crash; `⌘0` restores settings size
- [ ] `⌘N` opens a second independent window
- [ ] Dirty close prompts; Cancel keeps the pane
- [ ] Path click opens existing files; missing paths no-op
- [ ] `printf '\a'` respects `bell`
- [ ] Settings UI can toggle `copy_on_select`
- [ ] Tests/build green for touched packages
